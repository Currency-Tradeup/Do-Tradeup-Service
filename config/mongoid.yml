development:
  clients:
    default:
      database: Tradeup
      hosts:
        - localhost:27017
      options:
        server_selection_timeout: 1

production:
  clients:
    default:
      database: Tradeup
      hosts:
        - "<%= ENV['HOSTNAME'] %>"
      options:
        user: "<%= ENV['PASSWORD'] %>"
        password: "<%= ENV['USERNAME'] %>"
        ssl: true
        replica_set: tradeup-chains
        ssl_ca_cert_string: "<%= ENV['CA_CERT'] %>"
        ssl_verify: true